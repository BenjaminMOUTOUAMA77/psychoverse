class Formulaire{
  String? id;
  String? nom;
  String? description;
  String? inclusion;
  String? modele;
  String? asm_id;
  String? is_global;

  Formulaire(this.id, this.nom, this.description, this.inclusion, this.modele,
      this.asm_id, this.is_global);
}
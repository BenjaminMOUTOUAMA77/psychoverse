class Patient_type_fait_autre {
  String? id;
  String? nom;
  String? type_fait;
  String? patient_id;
  String? asm_id;

  Patient_type_fait_autre(
      this.id, this.nom, this.type_fait, this.patient_id, this.asm_id);
}
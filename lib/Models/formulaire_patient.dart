class Formulaire_patient {
  String? id;
  String? contenu;
  String? formulaire_id;
  String? patient_id;
  String? asm_id;

  Formulaire_patient(
      this.id, this.contenu, this.formulaire_id, this.patient_id, this.asm_id);
}